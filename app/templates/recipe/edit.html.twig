{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.cookbook'|trans({'%id%': recipe.id|default('')}) }}
{% endblock %}

{% block body %}
    {% include 'default/_form.html.twig' with {
        page_title: 'title.edit'|trans({'%id%': recipe.id|default('')}),
        form: form,
        submit_label: 'action.edit'|trans,
        back_to_list_path: 'recipe_index',
    } %}
{% endblock %}